import { createFileRoute } from "@tanstack/react-router";
import { Bell, Eye, LockKeyhole, SlidersHorizontal, UserRound } from "lucide-react";
import type { ReactNode } from "react";
import { PageHeading } from "@/components/page-heading";
import { Button } from "@/components/ui/button";

export const Route=createFileRoute("/settings")({head:()=>({meta:[{title:"Settings — DRISHTI"},{name:"description",content:"Configure analyst workspace preferences."},{property:"og:title",content:"Settings — DRISHTI"},{property:"og:description",content:"Configure analyst workspace preferences."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:SettingsPage});
const rows:[ReactNode,string,string][]=[[<UserRound/>,"Profile","Analyst identity and workspace details"],[<Eye/>,"Appearance","Display density and visual preferences"],[<SlidersHorizontal/>,"Watch Defaults","Default platforms and monitoring window"],[<Bell/>,"Notifications","Signal priority and notification preferences"],[<LockKeyhole/>,"Data & Privacy","Retention controls and aggregate-only safeguards"]];
function SettingsPage(){return <><PageHeading title="Settings" description="Essential preferences for your analysis workspace."/><section className="max-w-5xl border-y border-border bg-card">{rows.map(([icon,title,detail],i)=><div key={title} className={`flex items-center gap-5 px-5 py-5 ${i?"border-t border-border":""}`}><span className="text-primary [&_svg]:size-5">{icon}</span><div><h2 className="text-base font-semibold">{title}</h2><p className="mt-1 text-sm text-muted-foreground">{detail}</p></div><Button className="ml-auto" variant="outline" size="sm">Configure</Button></div>)}</section><div className="mt-8 max-w-5xl border-l-2 border-primary bg-accent/60 px-5 py-4"><p className="text-sm font-semibold text-primary">Responsible analysis by default</p><p className="mt-1 text-sm leading-6 text-muted-foreground">Demographic views remain aggregate-only. Individual demographic identification is not displayed.</p></div></>}