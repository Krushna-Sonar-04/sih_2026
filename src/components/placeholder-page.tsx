import { PageHeading } from "./page-heading";
import { Clock3, Database, ShieldCheck } from "lucide-react";
import type { ReactNode } from "react";
export function PlaceholderPage({title,description,eyebrow}:{title:string;description:string;eyebrow:string}){return <><PageHeading title={title} description={description}/><section className="border border-border bg-card"><div className="border-b border-border px-5 py-3 text-[10px] uppercase tracking-[0.14em] text-primary">{eyebrow}</div><div className="grid gap-px bg-border md:grid-cols-3"><Info icon={<Clock3/>} label="Frontend state" value="Prepared for Part 2"/><Info icon={<Database/>} label="Data source" value="Simulated dataset"/><Info icon={<ShieldCheck/>} label="Privacy posture" value="Aggregate-only design"/></div><div className="p-10"><div className="max-w-lg"><h2 className="text-base font-medium">Workspace ready for connection</h2><p className="mt-2 text-xs leading-5 text-muted-foreground">This Part 1 view establishes the navigation, hierarchy and operational state without implying live platform access.</p></div></div></section></>}
function Info({icon,label,value}:{icon:ReactNode;label:string;value:string}){return <div className="bg-card p-5"><div className="text-primary [&_svg]:size-4">{icon}</div><div className="mt-4 text-[9px] uppercase text-muted-foreground">{label}</div><div className="mt-1 text-xs">{value}</div></div>}
