# Frontend environment (safe values only — never put credentials in VITE_ variables).
VITE_API_BASE_URL=http://localhost:8000

# Local PostgreSQL credentials used by docker-compose.yml.
POSTGRES_USER=drishti
POSTGRES_PASSWORD=drishti_local_dev
POSTGRES_DB=drishti
POSTGRES_PORT=5432

# Backend credentials (Telegram / X / LLM) live ONLY in backend/.env.
# See backend/.env.example.
